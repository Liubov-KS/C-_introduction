// В предыдущем задании во всю гонял метод Math.Abs...
Console.Write("Введите число ");
double number = Convert.ToDouble(Console.ReadLine());
if (number >= 0)
{
    Console.Write(number);
}
else
{
    Console.Write(-number);
}